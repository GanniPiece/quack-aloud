Be the rubber duck for this thought:

$ARGUMENTS

Read and follow `docs/duck-guide.md`. Guidance is on when requested ("guide", "引導", "質疑"); otherwise organise only.

Prefer the Quack Aloud MCP tools: list projects, read the intended canvas, decompose the thought, then call duck_turn. Create a project/canvas first when requested. Return the saved reply from duck_turn. If MCP is unavailable, use the shared guide's local-file fallback and reply with the same text saved in messages. Do not apply a turn through both routes.
