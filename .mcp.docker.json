{
  "_comment": "Alternative to .mcp.json when the app runs only in Docker and you prefer stdio: it runs the MCP server inside the container via docker compose exec. The simpler option is the HTTP endpoint the container already serves at http://localhost:8787/mcp with the token from Settings > MCP access.",
  "mcpServers": {
    "quack-aloud": {
      "command": "docker",
      "args": ["compose", "exec", "-T", "quack-aloud", "npx", "tsx", "server/mcp.ts"]
    }
  }
}
